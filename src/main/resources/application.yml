name: deployProject

server:
  tomcat:
    connection-timeout: 600000   # 10분
  port: 9090

spring:
  main:
    headless: false
  mvc:
    async:
      request-timeout: 120000
  datasource:
    url: jdbc:mysql://backjin.iptime.org:3306/deployDB
    username: deployMan
    password: deploy123



  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  jooq:
    sql-dialect: MYSQL

deploy:
  client:
    ui-mode: ${DEPLOY_PROJECT_UI_MODE:DOWNLOAD}
    server-api-mode: ${DEPLOY_PROJECT_SERVER_API_MODE:LOCAL}
    remote-api-base-url: ${DEPLOY_PROJECT_REMOTE_API_BASE_URL:http://backjin.iptime.org:9090}
    installer-download-url: ${DEPLOY_PROJECT_INSTALLER_DOWNLOAD_URL:http://backjin.iptime.org:9090/download/deploy-project.exe}
  download:
    installer-path: ${DEPLOY_PROJECT_INSTALLER_PATH:./DeployProject.exe}
    installer-file-name: ${DEPLOY_PROJECT_INSTALLER_FILE_NAME:DeployProject.exe}

fgivpnT5008:
  iptime:
    org:
